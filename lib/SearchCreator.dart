import 'package:flutter/material.dart';

class SearchCreator extends StatelessWidget{
  @override
  Widget build(BuildContext context)
  {
     return Scaffold(
       appBar : AppBar(
           title : Text("Search Your Creator"),
       ),
     );
  }
}